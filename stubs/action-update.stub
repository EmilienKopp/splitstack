<?php

namespace {{ namespace }};

use App\Domain\Entities\{{ entity }}Entity;
use App\Infrastructure\Contracts\{{ repo }}RepositoryInterface;

class {{ class }}
{
    public function __construct(
        private readonly {{ repo }}RepositoryInterface ${{ lower }},
    ) {}

    public function execute({{ entity }}Entity $entity, array $data): {{ entity }}Entity
    {
        // TODO: apply $data mutations to $entity

        return $this->{{ lower }}->save($entity);
    }
}
